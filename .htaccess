# Redirect all web traffic to the Laravel public directory
RewriteEngine On

# Ensure RewriteBase is correct for your domain
RewriteBase /

# Send everything to Laravel's public folder
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]
